dist: xenial
jobs:
  include:
    - name: build
      script:
        - echo "Build"
        - ping -c 4 51.158.119.106 
        - ssh-keyscan -p 443 distribution.akeneo.com >> /tmp/known_hosts
        - cat /tmp/known_hosts
