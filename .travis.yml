dist: trusty

addons:
  apt:
    packages:
      - libxml2-utils
  snaps:
  - name: google-cloud-sdk
    confinement: classic
  - name: vault
    confinement: classic


jobs:
  include:
    - name: build
      script:
        - echo "Build"
        - echo "$TRAVIS_EVENT_TYPE"
    - name: deploy
      if: branch = master
      script:
        - echo "deploy"
        - echo "$TRAVIS_EVENT_TYPE"

