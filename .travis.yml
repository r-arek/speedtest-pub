dist: trusty

addons:
  snaps:
  - name: google-cloud-sdk
    confinement: classic
  - name: vault
    confinement: classic


jobs:
  include:
    - name: build
      script:
        - echo "Build"
        - echo "$TRAVIS_EVENT_TYPE"
    - name: deploy
      if: branch = master
      script:
        - echo "deploy"
        - echo "$TRAVIS_EVENT_TYPE"

