dist: xenial

addons:
  snaps:
  - name: google-cloud-sdk
  - name: vault

jobs:
  include:
    - name: build
      script:
        - echo "Build"
        - echo "$TRAVIS_EVENT_TYPE"
        - vault help
    - name: deploy
      if: branch = master
      script:
        - echo "deploy"
        - echo "$TRAVIS_EVENT_TYPE"
        - vault help

